schema_version: 1
repository_path: rtl
run_artifact_dir: .rtl-agent/runs
allowed_working_paths:
  - .
protected_paths: []
execution:
  timeout_seconds: 60
  max_output_bytes: 1048576
commands:
  seeded-failure:
    argv:
      - sh
      - sim/run.sh
    cwd: .
    timeout_seconds: 60
